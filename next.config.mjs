/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/Lishen-Amaraweera-Resume',
  assetPrefix: '/Lishen-Amaraweera-Resume/',
  output: 'export',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
